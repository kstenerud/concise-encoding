get_filename_component(CTE_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include(CMakeFindDependencyMacro)

list(APPEND CMAKE_MODULE_PATH ${CTE_CMAKE_DIR})

if(NOT TARGET CTE::CTE)
    include("${CTE_CMAKE_DIR}/CTETargets.cmake")
endif()

set(CTE_LIBRARIES CTE::CTE)
