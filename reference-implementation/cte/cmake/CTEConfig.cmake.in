get_filename_component(_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include(CMakeFindDependencyMacro)

list(APPEND CMAKE_MODULE_PATH ${_CMAKE_DIR})

if(NOT TARGET ::)
    include("${_CMAKE_DIR}/Targets.cmake")
endif()

set(_LIBRARIES ::)
